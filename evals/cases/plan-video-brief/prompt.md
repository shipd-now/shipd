/s:plan report-json-export

A video intent brief has already been ingested and installed at bundle slug
`report-json-export` (`.shipd/video/report-json-export/brief.md`) — the
recording has already been processed, so do not attempt to re-run
video-ingest. Read the installed brief through the engine and use its
intents as your investigation input ahead of the codebase-first read, exactly
as the video entry point describes, then investigate `src/report.py` as
usual and plan the change it describes end to end.

Plan this change end to end.
