# Third-party dependencies for two scoped surfaces only: the delivery board's
# `tui` verb (plugins/s/skills/build/scripts/dashboard.py) and declared-pipeline
# validation, which imports pydantic lazily on that path alone. The rest of the
# spec engine stays stdlib-only Python 3 — see .shipd/constitution.md.
# Both ranges below are mirrored in plugins/s/skills/doctor/SKILL.md's remedy
# table (a marketplace consumer has no checkout to `-r` from) — change both.
textual>=8.2.8,<9
pydantic>=2.12,<3
