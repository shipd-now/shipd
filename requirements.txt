# Third-party dependency for one scoped surface only: the delivery board's
# `tui` verb (plugins/s/skills/build/scripts/dashboard.py). The rest of the
# spec engine stays stdlib-only Python 3 — see .shipd/constitution.md.
# The range below is mirrored at two further sites — a consumer of the
# plugin has no checkout to `-r` from — and all three change together:
#   * plugins/s/skills/doctor/SKILL.md's remedy table
#   * PINNED_SPECIFIERS in plugins/s/bin/shipd, the `pip install` hint the
#     textual doctor check falls back to without a requirements.txt
textual>=8.2.8,<9
