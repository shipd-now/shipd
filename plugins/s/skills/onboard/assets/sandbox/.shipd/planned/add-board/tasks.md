## 1. Implement the read views

- [ ] 1.1 [req: list-cards] Implement the `list` command in `kanban.py`,
      printing one `#<id> [<lane>] <title>` line per card.
- [ ] 1.2 [req: board-view] Implement the `board` command in `kanban.py`,
      rendering three columns headed TODO / DOING / DONE with each card's title
      under its lane.
- [ ] 1.3 [req: list-cards, board-view] Seed `cards.json` with three sample
      cards, one in each lane, so both views render.
