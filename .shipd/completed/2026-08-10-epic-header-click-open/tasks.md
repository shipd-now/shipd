# Tasks

- [x] 1. [req: board-epic-grouping] In `plugins/s/skills/build/scripts/dashboard.py`, add the imports the new widgets need: `from textual.widgets.collapsible import CollapsibleTitle`, `from textual import on`, and `from textual.message import Message` (place them in the existing guarded `try:` textual-import block).
- [x] 2. [req: board-epic-grouping] In `dashboard.py`, add an `EpicCollapsibleTitle(CollapsibleTitle)` class carrying `epic_slug` (set in `__init__`) with a nested `OpenEpic(Message)` (carrying `epic_slug`), overriding `_on_click(self, event)` to `event.stop()` then post `self.Toggle()` when `event.offset.x <= 1` (the left pad + leading arrow cell) else post `self.OpenEpic(self.epic_slug)`. Document the column math (default `padding: 0 1` puts the arrow at widget column 1).
- [x] 3. [req: board-epic-grouping] In `dashboard.py`, add an `EpicCollapsible(Collapsible)` class that accepts an `epic_slug` kwarg, calls `super().__init__(*children, title=…, collapsed=…, id=…, classes=…)`, then replaces `self._title` with `EpicCollapsibleTitle(label=title, collapsed_symbol="▶", expanded_symbol="▼", collapsed=collapsed, epic_slug=epic_slug)`.
- [x] 4. [req: board-epic-grouping] In `dashboard.py` `_mount_epic_groups._flush`, build an `EpicCollapsible` (same `id="epic-group-<lane>-<slug>"`, `classes="epic-group"`, `collapsed=False`, `epic_slug=group_slug`) and remove the `≡` `menu_button` block entirely, so `EpicGroupRow` wraps only the collapsible. Update the `EpicGroupRow` class docstring to describe the single-collapsible header (no menu layer). Keep the `epic-group-row lane-item` classing.
- [x] 5. [req: board-epic-grouping] In `dashboard.py`, add a board-app handler `@on(EpicCollapsibleTitle.OpenEpic)` that pushes `EpicDetailScreen(event.epic_slug)` onto the board stack; remove the `_push_epic_menu` method and the `epic_menu_slug` branch in `on_button_pressed` (leave the search-clear, `group_mode_value`, `filter-add`, and `chip_kind` branches intact).
- [x] 6. [req: board-epic-grouping] In `dashboard.py`, remove the `EpicActionMenuScreen` class entirely.
- [x] 7. [req: board-epic-grouping] In `dashboard.py` `EpicDetailScreen.compose`, after the badge row, when `epic_is_runnable(epic)` and not `epic_stalled(epic)`, yield an action row containing `Button("Run epic", id="epic-run", classes="button-primary")`. In `EpicDetailScreen.on_button_pressed`, handle `epic-run` by `self.app.push_screen(EpicRunConfirmScreen(self.epic_slug))` (do not dispatch directly); keep the existing `epic-detail-close` and `epic-retry` branches.
- [x] 8. [req: board-epic-grouping] In `dashboard.py` CSS, remove the `.epic-menu-button` rule, drop the `menu` layer from `.epic-group-row` (and remove the now-unused menu-layer plumbing), and change `.epic-group CollapsibleTitle`'s `padding-right: 4` to `padding-right: 1`.
- [x] 9. [req: board-epic-grouping] In `plugins/s/skills/build/tests_textual/test_dashboard.py`, remove the menu machinery and tests: the `_open_epic_menu`/`_choose_menu_item` helpers, the `EpicActionMenuTest` class, and every test asserting the `≡` control, its float geometry, the View/Run action-menu items, or menu anchor/dismiss behavior; and drop the `#menu-epic-*`/`.epic-menu-button` assertions from the standalone and initiative-header tests. Remove `EpicActionMenuScreen` from the modal-CSS registry test.
- [x] 10. [req: board-epic-grouping] In `test_dashboard.py`, rewrite the tests that reached the epic-detail modal through the menu's View epic so they instead click the header title off the arrow (e.g. `Offset(6, 0)` on the group's `CollapsibleTitle`) and assert `EpicDetailScreen` is pushed.
- [x] 11. [req: board-epic-grouping] In `test_dashboard.py`, add tests covering: (a) clicking the header title off the arrow opens `EpicDetailScreen` with `collapsed` unchanged; (b) clicking the arrow cell toggles `collapsed` and pushes no modal; (c) an epic header has no `.epic-menu-button`; (d) a runnable epic's detail modal has a `#epic-run` button whose press pushes `EpicRunConfirmScreen`, and a non-runnable epic's detail modal has no `#epic-run`.
- [x] 12. [req: board-epic-grouping] Bump the plugin version in `plugins/s/.claude-plugin/plugin.json` from `0.6.73` to `0.6.74`.
