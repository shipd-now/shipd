## 1. Engine resolution

- [x] 1.1 [req: change-bridge] In `plugins/s/skills/review/tests/test_semdiff_change.py`, add `test_archived_change_resolves`: in `setUp`'s temp repo, move `.shipd/planned/sample-change` into the temp repo's completed directory under the archive name 2026-01-01-sample-change, commit, run `change sample-change`, and assert exit 0, `out["location"] == "completed"`, `out["dir"]` ends with that archive name under completed, a delta scenario contains "sixth failed login", `out["tasks"]["total"] == 4`, `out["lint"]["findings"] == []`, and `"skipped" in out["lint"]`. Run the module and observe the new test fail with exit 1.
- [x] 1.2 [req: change-bridge] In the same test module, add `test_newest_archive_wins`: copy `.shipd/planned/sample-change` into the temp repo's completed directory twice, under the archive names 2026-01-01-sample-change and 2026-02-01-sample-change, delete the planned copy, commit, run `change sample-change`, and assert `out["dir"]` ends with the 2026-02-01 archive name under completed. Extend `test_aggregated_change_context` to assert `out["location"] == "planned"` and `"skipped" not in out["lint"]`. Extend `test_unknown_change_fails_clearly` to assert `"completed/" in err`.
- [x] 1.3 [req: change-bridge] In `plugins/s/skills/review/scripts/semdiff.py`, add `_resolve_change_dir(root, content_dir, change)` directly above `cmd_change`: return `("planned", <abs planned dir>)` when `os.path.isdir` holds, else `("completed", <last sorted glob match of completed/*-<change> that is a directory>)`, else `(None, None)`.
- [x] 1.4 [req: change-bridge] In `cmd_change`, replace the `planned`-only join with `_resolve_change_dir`; when location is `None`, call `die` with `change '<name>' not found under <content_dir>/planned/ or <content_dir>/completed/.`; compute `rel_dir = os.path.relpath(change_dir, root)`.
- [x] 1.5 [req: change-bridge] In `cmd_change`, run `sl.lint_change` only when location is `planned`; otherwise set `lint = {"findings": [], "skipped": "archived change: its deltas are already merged into verified/ and the linter runs over planned/ only"}`. Insert `"location": location` and `"dir": rel_dir` after `"change"` in the dumped object. Update the `change` subparser help and argument help to say the change may live under `planned/` or `completed/`, and update the module docstring line 17 and the `# --- change` banner comment to match.
- [x] 1.6 [req: change-bridge] Run `python3 -m unittest discover -s plugins/s/skills/review/tests -v` and confirm every test in `test_semdiff_change.py` passes, including the two new ones.

## 2. Guidance surfaces

- [x] 2.1 [req: spec-aware-review] In `plugins/s/skills/review/references/spec-aware.md`, rewrite the load-condition sentence and the trigger paragraph to state the three triggers: the user named a change, exactly one change exists under `planned/`, or the diff adds or edits a change directory under `planned/` or `completed/`. Add one sentence saying the slug is the directory name from the `files` output with any leading `YYYY-MM-DD-` date prefix stripped, and one sentence saying `change <name>` resolves an archived change and reports `location` and `dir`.
- [x] 2.2 [req: spec-aware-review] In `plugins/s/skills/review/SKILL.md`, rewrite the `spec-aware.md` row's Load-when cell on its single table line to: the user named a change, exactly one change exists under `planned/`, or the diff carries a change directory under `planned/` or `completed/`. Confirm `wc -l` on the file stays under 300.
- [x] 2.3 [req: spec-aware-review] In `plugins/s/harness/bodies/review.md`, rewrite step 8's opening sentence to name the same three triggers and to say the slug is the change directory name with any leading `YYYY-MM-DD-` date prefix stripped; keep the `cat change <change>` read and every other sentence of the step unchanged, and name no file under `plugins/s/skills/review/references/`.
- [x] 2.4 [req: *] Bump `version` in `plugins/s/.claude-plugin/plugin.json` from `0.6.231` to `0.6.232`, because this change edits files under `plugins/s/` and the plugin cache snapshot is keyed by version.
- [x] 2.5 [req: *] Run `python3 -m unittest discover -s plugins/s/skills/review/tests` and `python3 -m unittest discover -s plugins/s/skills/build/tests` and confirm both report OK; run `grep -n "references/" plugins/s/harness/bodies/review.md plugins/s/integrations/copilot/SKILL.md` and confirm no match.
