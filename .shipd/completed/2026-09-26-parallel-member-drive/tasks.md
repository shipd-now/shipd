## 1. Wave computation in the driver

- [x] 1.1 [req: member-wave-scheduling] In `plugins/s/skills/build/tests/test_autopilot.py`, add `WaveTest(AutopilotTestBase)` with a helper `plant_ready(self, slug, caps, under=None)` that writes `planned/<slug>/plan.md` (`# <slug>` then `Status: ready`) and `planned/<slug>/tasks.md` (one unchecked task) plus `planned/<slug>/specs/<cap>/spec.md` per cap, under the temp root's content directory or, when `under` is given, under the content directory of the temp root's `.worktrees` entry named by `under`. Add `test_plan_waves_chunk_risk_ascending` (unplanned high, low, low, medium; `compute_waves(self.root, "ep", 2)` yields plan waves `[[low1, low2], [medium, high]]`), `test_disjoint_capabilities_share_a_build_wave` (ready `a`:{x}, `b`:{y}, `c`:{x,z}; cap 3 yields build waves `[[a, b], [c]]`), `test_cap_splits_a_wave` (four pairwise-disjoint ready members; cap 3 yields sizes 3 and 1), `test_capabilities_read_from_hosting_worktree` (a ready member planted with `under=slug`; its `capabilities` list the worktree's specs), `test_parallel_one_is_serial` (cap 1 yields one member per wave, unplanned risk-ascending then ready risk-ascending), and `test_waves_cli_drives_nothing` (`autopilot.main(["ep", "--root", self.root, "--waves", "--json"])` returns 0, stdout parses as JSON with `waves` and `skipped`, and no `autopilot/` heartbeat file exists under `.shipd`). Observe them fail.
- [x] 1.2 [req: member-wave-scheduling] In `plugins/s/skills/build/scripts/autopilot.py`, after `select_and_order`, add `member_capabilities(root, slug)`: `state, hosting = ss._member_state_with_root(root, slug)`; return `[]` unless `state == "ready"`; else return the sorted names of subdirectories of `os.path.join(sc.specs_dir(hosting), "planned", slug, "specs")` that contain a `spec.md`, or `[]` when that directory is absent. Add `compute_waves(root, epic, parallel)` per `plan.md`'s Implementation: validate the epic file exists (raise `AutopilotError` otherwise), `members = parse_members(root, epic)`, `to_drive, skipped = select_and_order(members)`; plan waves chunk `to_drive` by `parallel`; build waves greedily place `ready` members from `skipped` ordered by `(RISK_RANK.get(risk, len(RISK_RANK)), order)` into the first wave whose capability union is disjoint and whose size is below `parallel`; wave dicts carry `index` (1-based across both kinds), `stage`, and `members` as `{"slug", "risk", "capabilities"}`; `skipped` lists `{"member", "state"}` for every member whose state is neither `unplanned` nor `ready`. Return `{"epic": epic, "parallel": parallel, "waves": [...], "skipped": [...]}`.
- [x] 1.3 [req: member-wave-scheduling] In `main`, add `--waves` (store_true), `--parallel` (type int, default 3), and `--json` (store_true). Before dispatch, `parser.error` when `--parallel` is below 1, and when `--waves` is combined with `--dry-run` or `--member`. When `--waves` is set, call `compute_waves(os.path.abspath(args.root), args.epic, args.parallel)`; with `--json` print `json.dumps(result)` on one line; otherwise print `Waves for epic '<epic>' (cap <N>):`, then per wave `  wave <index> [<stage>]: ` followed by the members joined by `, `, each rendered `<slug> (risk <risk>)` for plan waves and `<slug> (<caps joined by ' '>)` for build waves, then `  skipped: <member> (<state>)` per skipped member; return 0. Extend the module docstring's summary and the `--dry-run` help text neighbourhood to name `--waves`.
- [x] 1.4 [req: member-wave-scheduling] Run `python3 -m unittest discover -s plugins/s/skills/build/tests -p test_autopilot.py` and confirm every test passes, the six new ones included.

## 2. The skill and the harness body

- [x] 2.1 [req: deliver-skill] In `plugins/s/skills/autopilot/SKILL.md` Phase 2, add a control bullet `--parallel N` — how many members the in-session drive runs concurrently (default 3; 1 is a serial run) — and, for the in-session mode, the instruction to run `python3 "${CLAUDE_PLUGIN_ROOT}/skills/build/scripts/autopilot.py" <epic> --waves --parallel N` and surface its wave lines in the confirmation alongside the dry-run member list. Note that the detached mode ignores `--parallel`.
- [x] 2.2 [req: in-session-drive] In the same file, rewrite the "Member order and pipeline" section so the in-session drive obtains grouping and order from `autopilot.py <epic> --waves --parallel N --json`, reading the `waves` array (each with `index`, `stage`, `members`) and the `skipped` array, and never parses the dry run's `Member order` or `skipped:` lines; keep the entry list coming from `pipeline-show --json` unchanged. Add a "Running a wave" subsection after "Per-member setup" stating the loop from `plan.md`'s Implementation: per-member setup for every member of the first wave; spawn the current entry's sub-agent for each member together in the background (a command entry runs via Bash in that member's worktree instead); wait for all; grade each from the grading table; advance each passing member to its next entry; repeat until every member of the wave has finished its walk; then re-run `--waves` and continue with its first wave until none is returned. State that `--parallel 1` reproduces a one-member-at-a-time walk. Remove the sentence in the "Stage options declared by the resolved entry" section that says members run one at a time.
- [x] 2.3 [req: in-session-asks-human] In the "Failure contract: ask, never park" section, replace the closing paragraph with: a failed grade or gate rejection inside a wave lets the wave's other in-flight sub-agents run to their end and be graded, starts no further entry for any member and no further wave, and then puts every stopped member and stage to the user in one report; the drive resumes with those members once the user answers. In "The in-session run summary", add that shipped members are reported per wave.
- [x] 2.4 [req: deliver-skill] In `plugins/s/harness/bodies/autopilot.md`, add a `--parallel N` row to the run-controls table (`how many members the in-session drive runs concurrently`, default `3`) and one sentence after the table saying the in-session drive runs members in waves the driver's `--waves` mode computes from their planned capabilities. Run `python3 -m unittest discover -s plugins/s/skills/build/tests -p test_harness_generate.py` and confirm OK.

## 3. Version and full suite

- [x] 3.1 [req: *] Run `git fetch origin main`, read `version` from `git show origin/main:plugins/s/.claude-plugin/plugin.json`, and bump `plugins/s/.claude-plugin/plugin.json` to one patch level above it, because this change edits files under `plugins/s/` and the plugin cache snapshot is keyed by version.
- [x] 3.2 [req: *] Run `python3 -m unittest discover -s plugins/s/skills/build/tests` from the repo root and confirm it reports OK.
