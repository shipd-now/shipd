## 1. Engine helper

- [x] 1.1 [req: current-spec-selection] In `plugins/s/skills/build/tests/test_spec_common.py`, add a `LocalStateTest` class with a temp root per test. Add `test_local_state_rules_default_dir` asserting `sc.local_state_rules(root) == [".shipd/state.json", ".shipd/autopilot/"]`; `test_local_state_rules_configured_dir` writing `{"dir": "specs"}` to `<root>/.shipd-config.json` and asserting both returned rules start with the configured `specs` name followed by a slash, ending in `state.json` and `autopilot/` respectively; `test_local_state_rules_external_store_is_empty` patching `sc.specs_dir` to return a path outside the root and asserting `[]`; `test_ensure_untracked_appends_rules_and_untracks` running `git init`, committing `.shipd/state.json` and `.shipd/autopilot/demo-build-heartbeat.json` (with `-c user.name=t -c user.email=t@x`), calling `sc.ensure_local_state_untracked(root)`, and asserting `.gitignore` ends with both rules, the return is `([(".shipd/state.json", True), (".shipd/autopilot/", True)], [".shipd/state.json", ".shipd/autopilot/demo-build-heartbeat.json"])`, `git ls-files .shipd` prints nothing, and both files still exist; `test_ensure_untracked_is_idempotent` calling it twice and asserting the second return is `([(".shipd/state.json", False), (".shipd/autopilot/", False)], [])` with `.gitignore` unchanged; `test_ensure_untracked_without_git_dir_only_appends` on a root with no `.git` asserting the rules are appended and `untracked == []`; `test_gitignore_carries` asserting `sc.gitignore_carries(root, ".shipd/state.json")` is False on a missing file, True after `ensure_gitignore_line`, and False for a line that sits only inside the members block. Run the module and observe the new tests fail.
- [x] 1.2 [req: current-spec-selection] In `plugins/s/skills/build/scripts/spec_common.py`, directly after `ensure_gitignore_line`, add `gitignore_carries(target, line)` by extracting the presence test `ensure_gitignore_line` already performs (lines outside the members block only) and make `ensure_gitignore_line` call it. Add `local_state_rules(root)`: compute `rel = os.path.relpath(specs_dir(root), root)`, return `[]` when `rel` starts with `..` or is absolute, else `[rel + "/state.json", rel + "/autopilot/"]` with `os.sep` replaced by `/`. Add `tracked_local_state(root)`: return `[]` unless `os.path.exists(os.path.join(root, ".git"))`; run `["git", "-C", root, "ls-files", "-z", "--", <rel>/state.json, <rel>/autopilot]` through `subprocess.run` with `capture_output=True`; on `OSError` or non-zero exit return `[]`; else return the NUL-split non-empty paths. Add `ensure_local_state_untracked(root)`: rules from `local_state_rules`; when empty return `([], [])`; `appended = [(rule, ensure_gitignore_line(root, rule)) for rule in rules]`; `tracked = tracked_local_state(root)`; when non-empty run `["git", "-C", root, "rm", "--cached", "-r", "-q", "--ignore-unmatch", "--", *tracked]`, swallowing `OSError` and treating a non-zero exit as nothing untracked; return `(appended, tracked_that_were_removed)`. Document each with a docstring citing `statusline current-spec-selection`.
- [x] 1.3 [req: current-spec-selection] Run `python3 -m unittest plugins.s.skills.build.tests.test_spec_common` from the repo root, or `python3 -m unittest discover -s plugins/s/skills/build/tests -p test_spec_common.py`, and confirm every test passes including the seven new ones.

## 2. The use and init verbs

- [x] 2.1 [req: current-spec-selection] In `plugins/s/skills/build/tests/test_spec_status.py` class `UseCurrentTest`, add `test_use_seeds_ignore_rules_and_untracks_a_tracked_state_file`: `git init` the root with a committed `.shipd/state.json` holding `{"current_spec": "old"}`, `make_change("dark-mode", status="ready")`, run `use dark-mode`, and assert exit 0, `stdout.strip() == "dark-mode"`, stderr contains `untracked .shipd/state.json`, `.gitignore` carries `.shipd/state.json` and `.shipd/autopilot/`, `git ls-files .shipd/state.json` prints nothing, and `self.state() == {"current_spec": "dark-mode"}`. Add `test_use_leaves_present_rules_alone`: a git root whose `.gitignore` already holds both rules and tracks nothing; run `use`; assert `.gitignore` bytes unchanged and stderr empty. Add `test_use_outside_a_checkout_still_records`: no `.git`; run `use`; assert exit 0 and the selection recorded. Observe the first test fail.
- [x] 2.2 [req: current-spec-selection] In `plugins/s/skills/build/scripts/spec_status.py` `cmd_use`, after the `_is_change` guard and before `write_current`, call `_appended, untracked = sc.ensure_local_state_untracked(root)` and write `"untracked %s\n" % path` to `sys.stderr` for each untracked path. Update the `use` line of the module docstring to say it also seeds the ignore rules and untracks a tracked state file.
- [x] 2.3 [req: layout-init-verb] In `TestInitVerb`, add `test_fresh_init_seeds_the_ignore_rules`: run `init` on a fresh root, assert `.gitignore` carries both rules and stdout lines between the sample line and the summary are exactly `ignored .shipd/state.json` then `ignored .shipd/autopilot/`. Add `test_rerun_reports_rules_exist`: run `init` twice, assert the second run prints `exists .shipd/state.json` and `exists .shipd/autopilot/` and `.gitignore` bytes are unchanged between runs. Add `test_init_untracks_tracked_local_state`: `git init` the root, commit `.shipd/state.json` and `.shipd/autopilot/demo-build-heartbeat.json`, run `init`, assert stdout carries `untracked .shipd/state.json` and `untracked .shipd/autopilot/demo-build-heartbeat.json` before the summary, `git ls-files .shipd` prints nothing, and both files still exist. Add `test_configured_dir_names_the_rules`: write `{"dir": "specs"}` to `<root>/.shipd-config.json`, run `init`, assert `.gitignore` carries two rules that start with the configured `specs` name followed by a slash and end in `state.json` and `autopilot/`, and carries no rule starting with `.shipd`. Observe them fail.
- [x] 2.4 [req: layout-init-verb] In `cmd_init`, after `_install_config_sample(root, specs)` and before `sc.stamp_schema_marker(root)`, call `appended, untracked = sc.ensure_local_state_untracked(root)`, print `"%s %s" % ("ignored" if added else "exists", rule)` per `(rule, added)`, then `"untracked %s" % path` per path. Extend the docstring and the `init` entry of the module docstring to name the ignore seeding and the untrack step.
- [x] 2.5 [req: layout-init-verb] Run `python3 -m unittest discover -s plugins/s/skills/build/tests -p test_spec_status.py` and confirm every test passes, including the existing `TestInitVerb` and `UseCurrentTest` cases.

## 3. Doctor check

- [x] 3.1 [req: doctor-local-state-check] In `plugins/s/skills/build/tests/test_shipd_cli.py` class `DoctorCheckTest`, insert `"local-state"` after `"store-sync"` in `ALL_CHECKS`, and add `test_default_checks_probe_local_state_after_store_sync` asserting `names.index("local-state") == names.index("store-sync") + 1` and `names.index("gh") == names.index("local-state") + 1`; extend the documented-order roster literal in `test_default_checks_run_in_the_documented_order` the same way. Add `test_tracked_state_file_warns_naming_init`: a temp git root with a committed `.shipd/state.json`; assert `shipd.check_local_state(root)` is `("warn", "local-state", detail)` with `.shipd/state.json` and `shipd init` in the detail. Add `test_missing_ignore_rule_warns`: git root tracking nothing, `.gitignore` holding only `.shipd/state.json`; assert `warn` naming `.shipd/autopilot/` and `shipd init`. Add `test_ignored_and_untracked_reports_ok`: both rules present, nothing tracked; assert `ok`. Add `test_non_checkout_skips_ok`: no `.git`; assert `ok` with `skipped` in the detail. Add `test_local_state_check_never_writes`: snapshot the root tree before and after and assert equality. In `DoctorFixTest`, add `test_local_state_finding_stays_report_only_under_fix`, modeled on `test_protection_finding_performs_no_gh_mutation_under_fix`, asserting the injected `run` receives no argv for the finding and the printed line names `shipd init`. Observe them fail.
- [x] 3.2 [req: doctor-local-state-check] In `plugins/s/bin/shipd`, add `check_local_state(root)` directly after `check_store_sync`: call `_load_engine()`; `rules = sc.local_state_rules(root)`; when empty return `("ok", "local-state", "content directory lives outside the repository — skipped")`; when `not os.path.exists(os.path.join(root, ".git"))` return `("ok", "local-state", "not a git checkout — skipped")`; `tracked = sc.tracked_local_state(root)`; when non-empty return `("warn", "local-state", "tracked by git: %s — run `shipd init` to ignore and untrack local state" % ", ".join(tracked))`; `missing = [r for r in rules if not sc.gitignore_carries(root, r)]`; when non-empty return `("warn", "local-state", "not ignored: %s — run `shipd init` to ignore and untrack local state" % ", ".join(missing))`; else return `("ok", "local-state", "ignored: %s" % ", ".join(rules))`. Insert `check_local_state(root)` into `default_checks` directly after `check_store_sync(root)` and before `check_gh()`. Add `"local-state": "the git-ignore of the local selection state — run `shipd init` to ignore and untrack it"` to `REMEDY_SURFACES`. Leave `AUTOMATED_REMEDIES` unchanged.
- [x] 3.3 [req: doctor-local-state-check] Run `python3 -m unittest discover -s plugins/s/skills/build/tests -p test_shipd_cli.py` and confirm every test passes, including every existing ordering assertion in `DoctorCheckTest`.

## 4. Docs, version, full suite

- [x] 4.1 [req: doctor-local-state-check] In `docs/getting-started.md`, in the sentence listing the doctor checks, insert `local-state` directly after `wiki`. In `docs/cheatsheet.md`, change the `init` row's description to `Create the content directory layout and ignore and untrack the local state files; safe to re-run.`
- [x] 4.2 [req: *] Bump `version` in `plugins/s/.claude-plugin/plugin.json` from `0.6.234` to `0.6.235`, because this change edits files under `plugins/s/` and the plugin cache snapshot is keyed by version.
- [x] 4.3 [req: *] Run `python3 -m unittest discover -s plugins/s/skills/build/tests` from the repo root and confirm it reports OK.
