## 1. README

- [x] 1.1 [req: harness-mode-docs] In `README.md`'s "Install mode" subsection,
      directly before the "Verify with" `shipd doctor` block, add a short
      paragraph documenting the interactive finish: on a terminal the curl
      install ends by running `shipd install`, which plays the animated SHIPD
      wordmark, offers a multi-select over the registry's twelve harnesses
      (read from `/dev/tty`, so it works under `curl | sh`), saves the
      selection to `~/.shipd/harnesses.json`, and generates user-global
      `shipd-*` command files for each selected harness that has a
      user-global command directory; headless runs (CI, redirected stdin)
      print a note and write nothing; re-running `shipd install` at any time
      reopens the selection preloaded from the record.
- [x] 1.2 [req: harness-mode-docs] Add a new `### Harness mode` subsection at
      the end of `README.md`'s Install section, after "Dev mode
      (working on shipd itself)", covering in order: what the mode is
      (generated `shipd-` prefixed command files let Cursor, Copilot,
      Windsurf, Codex, and the rest of the twelve registry harnesses drive
      the same `shipd` CLI); the feature-scaling model (each harness declares
      a subset of `subagents`, `question-dialogs`, `file-references`,
      `background-tasks`; one shared body per command is rendered per
      harness, so a generated file never mentions a feature its harness did
      not declare); inspection via fenced `shipd harness` and
      `shipd harness show <id>` examples; repo-level installs via a fenced
      `shipd harness add` example with `remove`, `--all`, `--user`, and
      `--root` noted — every generated file carries the ownership marker
      `<!-- generated by shipd harness; do not edit -->`, re-running `add`
      is idempotent, an unmarked file is refused without `--force`, and a
      harness with no surface for the selected mode is reported as skipped;
      and the one-sentence brand note (the animated wordmark appears only on
      the install surface; ☕ remains the brand mark).
- [x] 1.3 [req: harness-mode-docs] Add a `shipd harness [list|show <id>]` row
      to the verb table in `README.md`'s "The `shipd` CLI" section, worded
      after `shipd --help`'s "the harness-adapter registry" line; add a
      closing sentence to that section mirroring `shipd --help`'s `--json`
      line (the read verbs `list`, `status`, `locate`, `epic`, `workspace`,
      `lint`, and `harness` accept `--json`); and add `harness` to the
      existing `--json` read-verb list in `docs/quickstart.md` step 6, which
      is stale against `shipd --help`.

## 2. Quickstart

- [x] 2.1 [req: harness-mode-docs] In `docs/quickstart.md` step 1, after the
      auto-update paragraph, add a short paragraph: on a terminal the
      installer finishes with the interactive `shipd install` harness
      selection (headless runs skip it and print instructions instead;
      re-run `shipd install` later to reopen it), and inside a repository
      `shipd harness add` installs the generated command files repo-level —
      linking the README's harness-mode subsection for the details.

## Token usage breakdown

| Tool | Calls | Output tokens |
| --- | --- | --- |
| Bash | 62 | 27.1k |
| Edit | 14 | 7.5k |
| (no tool) | 0 | 5.9k |
| SendMessage | 4 | 3.8k |
| Read | 20 | 3.6k |
| Agent | 2 | 786 |
| ToolSearch | 2 | 453 |
| **Total** | 104 | 49.1k |
