## 1. Generation engine

- [x] 1.1 [req: dialect-rendering] Add
      `plugins/s/skills/build/tests/test_harness_generate.py` (unittest
      style) with rendering tests: cursor's `shipd-plan` frontmatter is
      exactly `name`/`id`/`category`/`description` with `name: shipd-plan`;
      github-copilot's carries only `description`; cline's file has no
      `---` block and begins `# shipd-plan` + description; values with `:`
      are YAML-quoted; the extension follows the surface pattern. Run and
      observe failure — the module does not exist yet.
- [x] 1.2 [req: dialect-rendering] Add
      `plugins/s/skills/build/scripts/harness_generate.py`: stdlib-only;
      imports `harness_registry` and `harness_bodies` (same directory);
      the fixed frontmatter value table from `plan.md`'s Implementation, a
      YAML escaper, per-dialect file rendering (frontmatter/header + the
      ownership marker `<!-- generated by shipd harness; do not edit -->`
      + the feature-rendered body), and surface resolution (repo pattern
      under a root; user-mode basename rule; refs roots
      `<content-dir>/harness/references/` and
      `~/.shipd/harness/references/`). The 1.1 tests pass.

## 2. File actions

- [x] 2.1 [req: harness-add-remove, harness-status] Extend the test file
      with action tests in temp roots and isolated `HOME`s:
      `add cursor --root` writes one owned file per body command plus the
      reference files with `{refs}` substituted; re-run is byte-identical;
      `add codex --user` writes under `~/.codex/prompts/` only;
      `add aider cursor` exits 0, generates cursor, reports aider skipped;
      an unmarked existing target refuses without `--force` (single
      `Error:` line, file unchanged) and rewrites with it;
      `remove cursor` deletes owned files, keeps an unmarked neighbor,
      prunes emptied dirs; `status` walks absent → installed → stale (after
      editing one file) and never writes; `status --json` parses keyed by
      id; `add` with no ids and no `--all` is a usage error (exit 2). Run
      and observe the new tests fail.
- [x] 2.2 [req: harness-add-remove, harness-status] Implement the actions
      in `harness_generate.py` (`add`, `remove`, `status` functions over a
      root/user mode) and wire them into `plugins/s/bin/shipd`'s
      `cmd_harness` (`action` choices gain `add`, `remove`, `status`;
      flags `--all`, `--root`, `--user`, `--force`; `--json` honored by
      `status`), delegating file work to the engine module via the
      on-demand import pattern. The 2.1 tests pass.
- [x] 2.3 [req: *] Run the CI suite command
      `python3 -m unittest discover -s plugins/s/skills/build/tests -v`
      without `textual`/`pydantic` installed and observe all tests pass —
      including the existing `test_harness_registry.py` read-verb tests,
      which must be unaffected.

## 3. Ship the snapshot

- [x] 3.1 [req: *] Bump `plugins/s/.claude-plugin/plugin.json` `version` to
      the next patch above the branch's post-base-merge value.

## Token usage breakdown

| Tool | Calls | Output tokens |
| --- | --- | --- |
| Bash | 76 | 37.7k |
| Edit | 18 | 26.9k |
| Write | 2 | 10.0k |
| Read | 13 | 5.6k |
| (no tool) | 0 | 4.3k |
| Agent | 2 | 940 |
| **Total** | 111 | 85.5k |
