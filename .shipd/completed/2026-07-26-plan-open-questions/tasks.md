## 1. Open-questions section

- [x] 1.1 [req: investigation-findings-digest] In
      `plugins/s/skills/plan/SKILL.md` Flow step 2, add the digest's
      closing section: when investigation leaves one or more genuinely open
      task-shaping questions, list them under an **OPEN QUESTIONS** header
      immediately before the go-ahead prompt — displayed, not asked; the
      user may answer them in the typed reply, and whatever stays open is
      settled in the post-gate rounds; omit the header when there are none.

## 2. Version

- [x] 2.1 [req: *] Bump `plugins/s/.claude-plugin/plugin.json` version from
      0.3.0 to 0.3.1 (plugin content changed: plan skill edited).
