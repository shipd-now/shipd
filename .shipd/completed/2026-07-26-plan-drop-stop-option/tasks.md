## 1. Drop the stop option

- [x] 1.1 [req: investigation-findings-digest] In
      `plugins/s/skills/plan/SKILL.md` Flow step 2, reduce the typed
      go-ahead prompt to two options — **1. proceed to the depth gate and
      planning** (recommended, named first), **2. adjust scope first** —
      removing "3. stop here" and the "on stop, end politely" follow-up.

## 2. Version

- [x] 2.1 [req: *] Bump `plugins/s/.claude-plugin/plugin.json` version from
      0.2.14 to 0.2.15 (plugin content changed: plan skill edited).
