# Tasks — epic-autopilot

## 1. Shared session driver

- [x] 1.1 [req: shared-session-driver] Add `plugins/s/skills/build/tests/test_session_driver.py` with failing tests using an injected fake runner: grade-passes-on-turn-2 stops at two turns; grade-never-passes exhausts `max_resumes` and returns the last session id; a turn failure surfaces `(ok=False, failure)`; the reply text is passed to resumed turns.
- [x] 1.2 [req: shared-session-driver] Add `plugins/s/skills/build/scripts/session_driver.py` (stdlib): `run_turn(prompt, cwd, resume_id, timeout, claude_bin, extra_args)` spawning `claude -p [--resume]` and extracting the session id from the JSON transcript, plus `drive(prompt, cwd, grade_fn, reply, max_resumes, timeout, runner=None)`. Tests from 1.1 pass.
- [x] 1.3 [req: shared-session-driver] Refactor `evals/run.py` to import the shared module for its turn/resume loop (path-insert the plugin scripts dir as it already does for grading), preserving behavior; `uvx pytest evals/tests/ -q` stays green.

## 2. Autopilot core

- [x] 2.1 [req: member-selection-and-order, run-report-and-controls] Add `plugins/s/skills/build/tests/test_autopilot.py` with failing tests for the pure logic (no live sessions): member selection picks only `unplanned` stub members ordered risk-ascending with table-order ties; `rejected`/`active`/`archived` members land in the report as skipped states; `--dry-run` prints member order + resolved pipeline and performs no action (fake runner asserts zero calls); `--max-members 1` reaches one member and reports the rest unreached; the JSON report accounts for every member under shipped/rejected/needs-human/skipped/unreached.
- [x] 2.2 [req: member-selection-and-order, run-report-and-controls] Add `plugins/s/skills/build/scripts/autopilot.py` (stdlib): arg parsing (`epic`, `--root`, `--max-members`, `--dry-run`, `--timeout`, `--max-resumes`, `--claude-bin`), epic preflight (exists, `ready`/`active`), stub-table parse + member selection/order via `spec_status` internals, dry-run printing, and the run-report writer (JSON file + human summary). Tests from 2.1 pass.
- [x] 2.3 [req: pipeline-stage-execution, three-strike-parking] Extend `test_autopilot.py` with failing stage-execution tests over an injected fake runner/gate/command executor: full pass drives plan (worktree created via the plugin helper path) → gate (exit 0) → build and records shipped; gate exit 2 parks as rejected with no retry and continues; a skipped gate entry runs no gate; a replaced build runs its command instead of a session; a custom entry between build and review runs in the worktree at its position; `tools` bindings appear in the driven prompt text with their fallback; a stage failing twice then passing proceeds; three failures park needs-human with stage, reason, and last session id while the run continues.
- [x] 2.4 [req: pipeline-stage-execution, three-strike-parking] Implement stage execution in `autopilot.py`: iterate `sc.resolve_pipeline(root)` entries covering plan/gate/build/custom (research/epic noted as pre-approval and ignored), member worktrees via `plugins/s/skills/build/scripts/worktree.sh`, plan/build grades per the delta (lint-clean `ready` plan; archived change + `gh pr view` PR), the three-strike re-drive with failure summaries appended, parking, and the end-of-run epic-sync close-out when any member PR merged. Tests from 2.3 pass.

## 3. The deliver skill

- [x] 3.1 [req: deliver-skill] Add `plugins/s/skills/deliver/SKILL.md`: preflight (epic at `ready`/`active` via `epic-show`, roster + `pipeline-show` output shown, run-control confirmation with the user), run `autopilot.py` in the foreground, relay the report verbatim plus `claude --resume <id>` pointers for needs-human members; register nothing else — the skill never plans, builds, or answers driven sessions. Bump `plugins/s/.claude-plugin/plugin.json` to `0.5.0`.

## 4. Verification

- [x] 4.1 [req: *] Suite barrier: full unittest suite green; library lint clean; `python3 plugins/s/skills/build/scripts/autopilot.py autonomous-delivery --dry-run` against this repo prints the real remaining members risk-ascending and the default pipeline, driving nothing.
- [x] 4.2 [req: *] Live micro-run (real model spend, 1-2 headless sessions): scaffold a scratch git repo (init + commit, no remote) with a minimal `.am` layout, a one-member epic at `ready` (trivial member: add a small greeting script per a simple stub), and a scratch `.shipd-config.json` declaring `{"autonomous-pipeline": [{"stage": "plan"}, {"stage": "gate"}, {"stage": "build", "replace": {"command": "touch built.txt", "fallback": "skip"}}]}` — so the run needs no `gh`/remote and exercises stage replacement for real. Run `autopilot.py <epic> --max-members 1 --timeout 900`; require: the member's plan emitted headlessly and gated (exit 0 path), the replaced build command ran in the member worktree (`built.txt` exists there), and the report accounts for the member; inspect, then remove the scratch repo.
