## 1. The guide

- [ ] 1.1 [req: getting-started-doc] Write `docs/getting-started.md` in this
      order: statusline registration (the snapshot-resolving install-mode
      command and the dev-mode repo path) with the meaning of each rendered
      segment; a `/s:plan` walkthrough explaining `plan.md`, the delta
      `specs/<capability>/spec.md`, and `tasks.md` with grammar-accurate
      excerpts; a `/s:build` walkthrough naming the `change/<name>` branch,
      the `verified/` merge, and the `completed/` archive as the durable
      outcomes; and a where-to-go-next section linking `quickstart.md`,
      `what-is-shipd.md`, `../.shipd/README.md`, and the README's skills
      table.

## 2. Verify

- [ ] 2.1 [req: getting-started-doc] Execute the documented install-mode
      statusline command verbatim against a populated plugin cache and
      confirm it renders the newest snapshot's statusline output.
- [ ] 2.2 [req: *] Re-read `docs/getting-started.md` end to end confirming
      every named command exists as documented in the repo and every relative
      link resolves; fix any drift found.
