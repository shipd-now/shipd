# Index

- [[binary-noun-verbs]] — the shipd binary's curated noun-verb surface and its exhaustive-enumeration consequences
- [[ci-safe-reference-lint]] — out-of-repo reference checks skip silently in workspace-less checkouts so CI never breaks
- [[discover-phase]] — the PRD as the discover phase's artifact and the optional Initiative → PRD → Epic → Change hierarchy
- [[prd-storage]] — workspace storage, chain resolution and shadowing, and the staged three-status PRD lifecycle
- [[prd-surfaces]] — the PRD's user surfaces: search, the shipd prd report with citing epics, roster, and render composition
- [[prd-template-tiers]] — the additive basic/standard/comprehensive section registry and why it nests
- [[prompt-convention-enforcement]] — prompt-prose conventions are guarded by stdlib engine tests, never shipped prose-only
- [[retrieval-surfaces]] — ranked term-hit retrieval: the frozen related contract and the superset search corpus
